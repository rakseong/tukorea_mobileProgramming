<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.kpu.termproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TermProject">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CheckActivity" android:label="Check Activity"/>
        <activity android:name=".CheckResultActivity" android:label="Checkresult Activity"/>
        <activity android:name=".DivisionActivity" android:label="Division Activity"/>
        <activity android:name=".CelebrityActivity" android:label="Celebrity Activity"/>
        <activity android:name=".MatchedActivity" android:label="Matched Activity"/>
        <activity android:name=".DBActivity"/>
    </application>

</manifest>